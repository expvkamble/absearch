/**
 * Created by vkamble on 6/6/16.
 */

/*
package contoller;

import java.util.Arrays;

import static java.lang.Integer.valueOf;
import static java.lang.Integer.valueOf;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.Set;

import org.springframework.util.CollectionUtils;
import org.springframework.util.StringUtils;
import org.springframework.web.bind.annotation.RequestBody;
import retrofit.http.GET;
import retrofit.http.Path;


@Path("/absearch/getactivity/")
public class AbSearchContoller {

    @GET
    public String get(
            ) {

        final RoomType roomType = roomTypeService.getRoomType(valueOf(propertyId), valueOf(roomTypeId));
        if (roomType == null) {
            return createResponseOkWithNotFound(null, Arrays.asList(valueOf(roomTypeId)));
        }
        return createResponseOk(roomType);
    }

}
*/